import javax.swing.*;

public class MenuFrame extends JFrame
{
    MenuFrame()
    {
        SportsPanel panel = new SportsPanel();

        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        this.add(panel);
        this.pack();
        this.setLocationRelativeTo(null);
        this.setVisible(true);
    }
}
